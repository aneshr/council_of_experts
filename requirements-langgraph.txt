# Use with the langchain-env conda environment (Python 3.10+).
#
# Install:
#   conda activate langchain-env
#   cd /path/to/chat_app
#   pip install -r requirements-langgraph.txt
#
# If you see "langchain 0.3.x requires langchain-core<1.0.0" after installing langgraph,
# upgrade to versions that support langchain-core 1.x:
#   conda activate langchain-env
#   pip install --upgrade "langchain>=0.4" "langchain-community>=0.3" "langchain-openai>=0.3"

fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# LangChain + LangGraph (langgraph requires langchain-core 1.x)
langchain-core>=1.0.0
langchain>=0.3.0
langchain-community>=0.3.0
langchain-openai>=0.3.0
langgraph>=0.2.0

PyPDF2>=3.0.0
python-docx>=1.0.0
